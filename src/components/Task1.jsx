import React from 'react';

const Task1 = () => {
  return (
    <p>
      JSX is a syntax extension for JavaScript that looks like HTML. It allows us to write HTML-like code within React components, making UI creation easier and more readable.
    </p>
  );
};

export default Task1;
